ingress-nginx:
  tcp:
    5432: "postgresql-homelab-stage/postgresql-homelab-stage:5432"

  controller:
    replicaCount: 2

    admissionWebhooks:
      enabled: true
      patch:
        enabled: true
        image:
          registry: registry.k8s.io
          image: ingress-nginx/kube-webhook-certgen
          tag: v1.4.0
      service:
        # 名稱不是預設 => 可指定
        name: ingress-nginx-controller-admission
        namespace: ingress-nginx-homelab-stage

    resources:
      requests:
        cpu: 100m
        memory: 128Mi

      limits:
        cpu: 300m
        memory: 256Mi