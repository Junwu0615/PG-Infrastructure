prometheus:
  replicaCount: 1

  server:
    ingress:
      enabled: true
      ingressClassName: nginx
      hosts:
        - prometheus.k8s.local

    persistentVolume:
      enabled: false

  alertmanager:
    persistentVolume:
      enabled: false