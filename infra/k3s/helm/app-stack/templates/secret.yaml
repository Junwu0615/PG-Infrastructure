# helm/app-stack/templates/secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
data:
  # 由 Helm 傳入 Base64 編碼後的字串
  DB_PASSWORD: {{ .Values.dbPassword | b64enc | quote }}