# helm/app-stack/templates/configmap.yaml
# 測試 5：配置更新自動觸發重啟 ( Reloader )

apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  DB_HOST: "postgres-service"
  DB_NAME: "myapp"
  APP_MODE: "stage"